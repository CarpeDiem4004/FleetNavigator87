- Rotas/Telas afetadas: {listar}
- Mudanças recentes: {commits/arquivos alterados}

---

## O que fazer (passo a passo)

1. **Localizar a origem do erro**
   - Verificar se os hooks estão sendo usados corretamente (no topo de componentes ou custom hooks).
   - Checar duplicatas do React e versões de `react` / `react-dom`.
   - Garantir que Providers (AuthProvider, Router, etc.) envolvem os hooks que dependem deles.
   - Conferir configuração de bundler (`vite.config.ts` ou `webpack.config.js`).
   - Validar `.env`, URLs e permissões de API.

2. **Hipóteses e verificação**
   - Listar 2–5 causas possíveis.
   - Indicar arquivos/linhas prováveis.
   - Explicar como **reproduzir** e **confirmar** cada hipótese.

3. **Correções propostas (com *diff*)**
   - Para cada causa confirmada, entregar patch em formato *diff*.
   - Incluir comandos para alinhar dependências:
     ```bash
     npm i react@latest react-dom@latest
     npm dedupe
     ```
   - Exemplo de ajuste de Vite:
     ```ts
     // vite.config.ts
     resolve: {
       alias: {
         react: path.resolve(__dirname, 'node_modules/react'),
         'react-dom': path.resolve(__dirname, 'node_modules/react-dom'),
       },
       dedupe: ['react', 'react-dom'],
     }
     ```

4. **Plano de validação**
   - Comandos para limpar e reconstruir:
     ```bash
     rm -rf node_modules package-lock.json
     npm i
     npm ls react react-dom
     npm run dev
     ```
   - Roteiro de testes manuais (UI + API).
   - Critérios de aceite (sem erros no console, tela X renderiza, API responde 200).

5. **Riscos e próximos passos**
   - Listar possíveis impactos colaterais.
   - Recomendar refactors futuros (padronizar hooks, corrigir imports, ajustar tipagens).

---

## Formato de saída esperado
1. **Diagnóstico resumido (1–2 parágrafos)**
2. **Causas-raiz** (bullets com arquivos/linhas)
3. **Como reproduzir** (passo a passo + comandos)
4. **Correções (diffs + comandos)**
5. **Plano de validação**
6. **Riscos e próximos passos**

---

> Observação: Se precisar de mais contexto, liste exatamente quais arquivos/trechos quer que eu cole aqui (com caminhos e linhas).