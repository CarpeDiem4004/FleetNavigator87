Perfeito — aqui vai um arquivo único para você colar na IA do Replit pedindo a volta das configurações do dia 01/08 sem perder nenhum dado, mexendo apenas na parte sistêmica (código e configs).

# Auditoria & Rollback Seguro (SEM PERDER DADOS)

Atue como *auditor sênior* (React + TS + Vite + Node + Supabase).  
Quero restaurar *somente as configurações/sistema* do *dia 01/08* e *NÃO* perder nenhum dado já inserido no banco. Faça uma análise e proponha o rollback de código/config *sem tocar em dados*.

## Regras invioláveis (dados)
- ❗ *Não* executar migrações destrutivas.
- ❗ *Não* alterar schemas/tabelas/índices existentes de forma incompatível.
- ❗ *Não* apagar/regenerar buckets do Supabase Storage.
- ❗ *Não* alterar/rotacionar chaves/variáveis de ambiente atuais.
- ❗ *Não* resetar o projeto Supabase.
- ✅ Somente *código e configs*: vite.config.ts, package.json, package-lock.json/pnpm-lock.yaml, tsconfig.json, código fonte (src/**), e scripts de build.

## Objetivo
1) *Carregar snapshot/configurações de 01/08* (ou o commit mais próximo) e *comparar* com o estado atual.  
2) *Listar diffs cirúrgicos* de configs e pontos de código que impactam boot/HMR/hooks.  
3) *Propor patch* para voltar ao estado estável *sem tocar no banco*.

## Escopo do que pode ser alterado
- ✅ client/package.json, client/vite.config.ts, client/tsconfig.json
- ✅ src/context/*, src/hooks/*, src/main.tsx, src/App.tsx
- ✅ supabase *apenas* clientes de código (ex.: supabase-compat.ts), *sem* mexer em schema.
- 🚫 supabase/migrations/** (apenas leitura; *não aplicar* para trás)
- 🚫 .env* (apenas validar presença; *não reescrever*)
- 🚫 Banco de dados/Storage do Supabase

## O que você deve entregar
1. *Diagnóstico resumido (1–2 parágrafos)*  
   - O que mudou entre *01/08* e hoje que explica o sistema não subir (ex.: mudança de @vitejs/plugin-react, HMR, alias/dedupe de React, imports, cliente Supabase).

2. *Diferenças críticas (com arquivos/linhas/versões)*  
   - Listar versões antigas vs atuais de react, react-dom, vite, @vitejs/plugin-react.  
   - Mostrar diffs em vite.config.ts (fastRefresh, alias, dedupe).  
   - Apontar mudanças em SupabaseAuthContext.tsx, useFetchWithAuth.ts, App.tsx.

3. *Patch proposto (apenas sistema, sem dados)*  
   - Forneça *diffs unificados* para:
     - client/vite.config.ts (plugin único @vitejs/plugin-react, fastRefresh estável, alias e dedupe de react/react-dom).  
     - client/package.json e *lockfile* para prender versões (sem downgrade de segurança).  
     - Ajustes em imports/hooks que causaram quebras.  
   - *Não* incluir alterações em migrations ou .env.

4. *Plano de execução seguro (comandos)*  
   - Criar branch de rollback e aplicar patch:
     bash
     git checkout -b chore/rollback-config-0108
     # aplicar diffs fornecidos por você
     npm ci || npm i
     npm ls react react-dom vite @vitejs/plugin-react
     npm run dev
     
   - Se necessário restaurar lockfile conforme 01/08 *sem* tocar no DB:
     bash
     rm -rf node_modules
     npm ci || npm i
     
   - *Dry-run* de qualquer script que toque Supabase (apenas listar; não executar).

5. *Plano de validação (sem risco a dados)*
   - Checklist:
     - App sobe sem $RefreshSig$ nem erros de hooks.
     - npm ls mostra *uma única* versão de react/react-dom.
     - Rotas principais abrem e *leem* dados existentes (somente leitura).  
     - *Nenhuma* operação de escrita/alteração em DB é executada durante validação.
     - Variáveis VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY reconhecidas (apenas leitura).

## Observações
- Se precisar comparar commits, liste os *hashes* relevantes e gere diffs focados somente em *código/config*.  
- Se identificar qualquer passo que possa afetar dados, *pare* e proponha alternativa que preserve 100% do conteúdo.

> Objetivo final: *voltar às configs estáveis de 01/08* para o app abrir normalmente, *sem perder nenhum dado* e *sem alterações em schema/migrações*.