Ótimo print — ele mostra exatamente onde está o problema 👍
A regra do ADMIN ainda não está sendo considerada no backend, por isso você recebe 401 / 403 mesmo logando corretamente.

Vou te explicar o que está errado, por que acontece e como pedir para a IA corrigir sem quebrar nada.

🔍 O que o erro está dizendo (em português claro)
No console:

401 Unauthorized

403 Forbidden

Endpoint:

/api/base-auth/login

Na tela:

"Você não tem permissão para acessar esta base"

👉 Isso significa que:

O login até acontece

Mas a validação da base está bloqueando

O backend só está checando user_bases

Não existe exceção para role = admin

🧠 Onde está o erro lógico (muito comum)

Hoje o fluxo está assim (errado para admin):

login OK
↓
buscar vínculo user_bases
↓
não encontrou vínculo
↓
403 Forbidden ❌


O correto deveria ser:

login OK
↓
buscar role do usuário
↓
SE role == 'admin' → liberar ✅
SENÃO → validar user_bases

✅ Correção CERTA (regra final)
Regra de ouro:

Admin nunca deve depender da tabela user_bases

🧠 Prompt CORRETIVO para a IA do Replit

👉 Cole isso agora para corrigir o erro do print:

Corrija a lógica de autorização do endpoint /api/base-auth/login para permitir acesso global a usuários com role admin.

Problema atual

O sistema está retornando 401/403 para usuários ADMIN

A validação está dependendo apenas da tabela user_bases

Correção obrigatória

Após autenticar o usuário:

Buscar o perfil do usuário (role)

Se role === 'admin', liberar acesso imediatamente

Não consultar user_bases para admins

Fluxo correto
if (user.role === 'admin') {
  return acesso_liberado
}

// demais usuários
validar vínculo em user_bases

Regras

Manter retorno 403 apenas para usuários não-admin sem vínculo

Não alterar layout, rotas ou URLs existentes

Garantir que admin possa acessar qualquer /bases/:baseId/public

Debug

Adicionar log claro no backend indicando:

user_id

role

base_id

motivo da liberação ou bloqueio

Ajustar o código de forma segura, limpa e comentada.

🧪 Como você valida que ficou certo (checklist)

Depois da correção:

✅ Usuário ADMIN:

Loga em qualquer base

Mesmo sem vínculo em user_bases

URL pode ser alterada manualmente

✅ Usuário NORMAL:

Só entra se estiver vinculado

Alterar URL gera 403

✅ Console:

❌ Não aparece mais 401/403 para admin

Logs mostram role: admin → access granted

💡 Dica profissional (vale ouro)

Se quiser deixar à prova de erro no futuro, a hierarquia ideal é:

role (admin manda em tudo)

vínculo base

permissões específicas (futuro)