import React from 'react';
import PostRedirect from '../PostRedirect';

const ABCRedirect: React.FC = () => {
  return <PostRedirect from="/public/posto/abc" to="/posto/abc" />;
};

export default ABCRedirect;