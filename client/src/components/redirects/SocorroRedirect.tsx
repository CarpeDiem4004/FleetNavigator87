import React from 'react';
import PostRedirect from '../PostRedirect';

const SocorroRedirect: React.FC = () => {
  return <PostRedirect from="/public/posto/socorro" to="/posto/socorro" />;
};

export default SocorroRedirect;