import React from 'react';
import PostRedirect from '../PostRedirect';

const GuarulhosRedirect: React.FC = () => {
  return <PostRedirect from="/public/posto/guarulhos" to="/posto/guarulhos" />;
};

export default GuarulhosRedirect;