import React from 'react';
import PostRedirect from '../PostRedirect';

const CampinasRedirect: React.FC = () => {
  return <PostRedirect from="/public/posto/campinas" to="/posto/campinas" />;
};

export default CampinasRedirect;